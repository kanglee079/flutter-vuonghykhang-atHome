import 'package:flutter/material.dart';

class MyOrderCancelPage extends StatelessWidget {
  const MyOrderCancelPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
